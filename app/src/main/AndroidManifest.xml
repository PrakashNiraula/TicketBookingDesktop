<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ticketer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".view_controller.OwnerDashboard"
            android:label="@string/title_activity_owner_dashboard"></activity>

        <activity
            android:name=".view_controller.UserDashboard"
            android:label="@string/title_activity_user_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view_controller.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view_controller.ViewSeats" />
        <activity android:name=".view_controller.VeiwBuses" />
        <activity android:name=".view_controller.ScheduleManager" />
        <activity android:name=".view_controller.BusDetails" />
        <activity android:name=".view_controller.Routes" />
        <activity android:name=".view_controller.RegisterActivity" />
        <activity android:name=".view_controller.MainActivity" />
    </application>

</manifest>